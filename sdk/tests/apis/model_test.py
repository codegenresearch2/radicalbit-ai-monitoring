import time\"import unittest\\\nimport uuid\\\nimport boto3\\"from moto import mock_aws\\"import pytest\\"import responses\\\nfrom radicalbit_platform_sdk.apis import Model, ModelFeatures\\"from radicalbit_platform_sdk.errors import ClientError\\"from radicalbit_platform_sdk.models import (ColumnDefinition, DataType, FieldType, Granularity, JobStatus, ModelDefinition, ModelType, OutputType, ReferenceFileUpload, SupportedTypes)\"\\n\\nclass ModelTest(unittest.TestCase):\\\n    @responses.activate\\\n    def test_delete_model(self):\\\n        base_url = 'http://api:9000'\\\n        model_id = uuid.uuid4()\\\n        column_def = ColumnDefinition(\\"name='column', type=SupportedTypes.string, field_type=FieldType.categorical\")\\\n        outputs = OutputType(prediction=column_def, output=[column_def])\\\n        model = Model(\\"base_url, ModelDefinition(uuid=model_id, name='My Model', model_type=ModelType.BINARY, data_type=DataType.TABULAR, granularity=Granularity.MONTH, features=[], outputs=outputs, target=column_def, timestamp=column_def, created_at=str(time.time()), updated_at=str(time.time())))')